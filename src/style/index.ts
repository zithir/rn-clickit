import { StyleSheet } from "react-native";

export default StyleSheet.create({
  heading: { fontSize: 32 },
  centerItems: { flex: 1, alignItems: "center", justifyContent: "center" }
});

export { default as COLORS } from "./_colors";
